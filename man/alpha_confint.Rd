% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qoi.R
\name{alpha_confint}
\alias{alpha_confint}
\title{Profile-likelihood interval for the dispersion parameter alpha}
\usage{
alpha_confint(object, level = 0.95)
}
\arguments{
\item{object}{A \code{"cpb"} object.}

\item{level}{Confidence level (default 0.95).}
}
\value{
A length-2 numeric vector (\code{lower}, \code{upper}) with attributes \code{alpha} (the
point estimate) and \code{boundary} (\code{TRUE} if the lower bound is at the feasibility
boundary, i.e. strong underdispersion, where the interval is one-sided).
}
\description{
Profile-likelihood interval for the dispersion parameter alpha
}
\examples{
set.seed(7); x <- rnorm(300)
N <- pmax(round(exp(1.5 + 0.4 * x) / 0.5), 1); y <- rbinom(300, N, 0.5)
fit <- cpb(y ~ x, data.frame(y = y, x = x)[y > 0, ], se = "none")
alpha_confint(fit)
}
