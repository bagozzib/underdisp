% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_families.R
\name{doublepois-distribution}
\alias{doublepois-distribution}
\alias{ddoublepois}
\alias{pdoublepois}
\alias{qdoublepois}
\alias{rdoublepois}
\title{Double Poisson distribution functions}
\usage{
ddoublepois(x, mu, theta, log = FALSE)

pdoublepois(q, mu, theta, lower.tail = TRUE, log.p = FALSE)

qdoublepois(p, mu, theta, lower.tail = TRUE, log.p = FALSE)

rdoublepois(n, mu, theta)
}
\arguments{
\item{x, q}{Vector of quantiles (non-negative integers).}

\item{mu}{Location parameter (scalar or vector, recycled).}

\item{theta}{Dispersion parameter (scalar, positive).}

\item{log, log.p}{Return log probabilities.}

\item{lower.tail}{If \code{TRUE} (default), \eqn{P(X \le x)}.}

\item{p}{Vector of probabilities.}

\item{n}{Number of draws.}
}
\value{
\code{ddoublepois} a density, \code{pdoublepois} a CDF, \code{qdoublepois} a
quantile, \code{rdoublepois} a numeric vector of count draws.
}
\description{
Density, distribution, quantile, and random generation for Efron's (1986)
double Poisson distribution with location \code{mu} and dispersion \code{theta},
normalized by the exact sum rather than Efron's closed-form approximation.
\code{theta > 1} gives underdispersion, \code{theta = 1} is the Poisson, \code{theta < 1}
overdispersion; the variance-to-mean ratio is approximately \code{1/theta}, and the
mean is close to (but not exactly) \code{mu}. The exact mean and variance are what
\code{count_reg(family = "doublepois")} reports through \code{predict()} and \code{fitted()}.
}
\examples{
ddoublepois(0:5, mu = 3, theta = 2)
sum(ddoublepois(0:60, mu = 3, theta = 2))
}
\references{
Efron, B. (1986). Double exponential families and their use in
generalized linear regression. \emph{Journal of the American Statistical
Association}, 81(395), 709-721.
}
\seealso{
\code{\link[=count_reg]{count_reg()}}
}
