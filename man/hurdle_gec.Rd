% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gec_variants.R
\name{hurdle_gec}
\alias{hurdle_gec}
\title{Hurdle GEC (Katz-family) regression}
\usage{
hurdle_gec(
  formula,
  data,
  participation = NULL,
  part_fe = NULL,
  link = c("logit", "probit", "cloglog"),
  offset = NULL,
  cluster = NULL,
  se = c("none", "bootstrap"),
  B = 500,
  max.support = 500
)
}
\arguments{
\item{formula}{Intensity formula, \code{y ~ x}.}

\item{data}{A data frame.}

\item{participation}{One-sided formula for the participation logit; defaults
to the intensity's right-hand side.}

\item{part_fe}{Optional column name for fixed effects in the participation
model (added as factor dummies).}

\item{link}{Link for the participation model: \code{"logit"} (default), \code{"probit"},
or \code{"cloglog"}.}

\item{offset}{Optional offset (log scale) for the intensity: a numeric vector
or a column name in \code{data}.}

\item{cluster}{Optional cluster (column name or vector) for the intensity's
cluster bootstrap.}

\item{se}{Intensity inference: \code{"none"} (default) or \code{"bootstrap"}.}

\item{B}{Bootstrap resamples.}

\item{max.support}{Guard on the maximum evaluated support.}
}
\value{
An object of class \code{"hurdle_gec"}.
}
\description{
Joins a participation (hurdle) logit to a zero-truncated GEC intensity on the
positive counts. Unlike \code{\link[=hurdle_cpb]{hurdle_cpb()}}, whose intensity is fixed to the
underdispersed CPB, the GEC intensity estimates its own dispersion direction.
}
\section{Limitation}{

Unlike \code{\link[=hurdle_cpb]{hurdle_cpb()}}, \code{hurdle_gec()} has no \code{fe} argument for a
concentrated fixed-effects intensity (no concentrated zero-truncated GEC is
implemented). For within-unit intensities, enter the unit factor as dummies
in \code{formula} (feasible for moderate unit counts), or use \code{\link[=hurdle_cpb]{hurdle_cpb()}}.
}

\examples{
\donttest{
set.seed(3); n <- 300; x <- rnorm(n); z <- rnorm(n)
y <- ifelse(rbinom(n, 1, plogis(0.3 + 0.8 * z)) == 1, rpois(n, exp(1 + 0.3 * x)) + 1L, 0L)
hurdle_gec(y ~ x, data.frame(y = y, x = x, z = z), participation = ~ z)
}
}
\seealso{
\code{\link[=hurdle_cpb]{hurdle_cpb()}}, \code{\link[=gec]{gec()}}, \code{\link[=zi_gec]{zi_gec()}}
}
