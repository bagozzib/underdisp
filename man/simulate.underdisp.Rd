% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate.underdisp}
\alias{simulate.underdisp}
\alias{simulate.cpb}
\alias{simulate.cpb_fe}
\alias{simulate.gec}
\alias{simulate.count_reg}
\alias{simulate.hurdle_cpb}
\alias{simulate.hurdle_gec}
\alias{simulate.hurdle_count}
\alias{simulate.zi_cpb}
\alias{simulate.zi_gec}
\alias{simulate.zi_count}
\title{Simulate responses from a fitted underdisp model}
\usage{
\method{simulate}{cpb}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{cpb_fe}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{gec}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{count_reg}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{hurdle_cpb}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{hurdle_gec}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{hurdle_count}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{zi_cpb}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{zi_gec}(object, nsim = 1, seed = NULL, ...)

\method{simulate}{zi_count}(object, nsim = 1, seed = NULL, ...)
}
\arguments{
\item{object}{A fitted model from this package.}

\item{nsim}{Number of replicate response vectors.}

\item{seed}{Optional seed, handled as in \code{\link[stats:simulate]{stats::simulate()}}: the caller's
RNG state is restored on exit when a seed is supplied.}

\item{...}{Unused.}
}
\value{
A data frame with \code{nsim} integer columns, one row per observation,
with a \code{"seed"} attribute.
}
\description{
Draws \code{nsim} replicate response vectors from the fitted model, at the
estimated parameters and the estimation data, in the format of
\code{\link[stats:simulate]{stats::simulate()}}. Available for every model class in the package:
\code{cpb}, \code{cpb_fe}, \code{gec}, \code{gec_fe} (through inheritance), \code{count_reg},
\code{hurdle_count}, \code{zi_count}, \code{hurdle_cpb}, \code{hurdle_gec}, \code{zi_cpb}, and
\code{zi_gec}. Two-part models first draw the binary stage (participation or
structural zero), then the count stage from its own distribution, so the
replicates carry the model's full zero structure.
}
\details{
The main consumer is simulated-residual diagnostics:
\code{DHARMa::createDHARMa(simulatedResponse = as.matrix(simulate(fit, 250)), observedResponse = y, fittedPredictedResponse = fitted(fit), integerResponse = TRUE)} works for any fit in the family.
}
\examples{
set.seed(1); x <- rnorm(200)
N <- pmax(round(exp(1.4 + 0.4 * x) / 0.5), 1); y <- rbinom(200, N, 0.5)
fit <- cpb(y ~ x, data = data.frame(y = y, x = x)[y > 0, ], se = "none")
sims <- simulate(fit, nsim = 5)
colMeans(sims)
}
