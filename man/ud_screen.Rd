% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ud_screen.R
\name{ud_screen}
\alias{ud_screen}
\title{Screen a count outcome for underdispersion}
\usage{
ud_screen(
  formula,
  data,
  run_cpb = TRUE,
  cpb_max_n = 3000,
  run_gp = TRUE,
  run_comp = TRUE,
  comp_max_par = 30,
  comp_max_n = 5000,
  ztp_threshold = c("calibrated", "bootstrap"),
  ztp_boot_B = 199L,
  digits = 3
)
}
\arguments{
\item{formula}{A model formula.}

\item{data}{A data frame.}

\item{run_cpb}{Logical; fit the CPB when the data are not zero-dominated
(default \code{TRUE}).}

\item{cpb_max_n}{Skip the CPB fit above this sample size (default 3000).}

\item{run_gp}{Logical; fit the generalized-Poisson comparator (default \code{TRUE}).}

\item{run_comp}{Logical; fit the native COM-Poisson comparator (default
\code{TRUE}). Skipped when the mean model carries more than \code{comp_max_par}
parameters (the COM-Poisson has no concentrated fixed-effects path, so
dummy-heavy screens would be slow) or when \code{n} exceeds \code{comp_max_n}.}

\item{comp_max_par, comp_max_n}{Parameter and sample-size gates for the
COM-Poisson comparator (defaults 30 and 5000).}

\item{ztp_threshold}{How to set the at-risk test's underdispersion cutoff.
\code{"calibrated"} (default) uses the simulation-calibrated rule
\eqn{1 - 2.27/\sqrt{n_+}}, whose size was established on designs without
fixed effects; \code{"bootstrap"} calibrates the cutoff on the data at hand by
a parametric bootstrap of the fitted zero-truncated Poisson null
(simulate \code{ztp_boot_B} at-risk panels at the fitted rates, refit, and take
the empirical 5\\% quantile of the statistic). The bootstrap is the
recommended choice when the mean model's parameter count is a nontrivial
share of the positive observations (dummy-heavy fixed-effects screens),
where the calibrated rule is anti-conservative.}

\item{ztp_boot_B}{Number of parametric-bootstrap replicates (default 199).}

\item{digits}{Printing precision.}
}
\value{
An object of class \code{"ud_screen"} with \code{verdict_marginal}, \code{verdict_atrisk},
the conditional and at-risk (ZTP-benchmarked) Pearson statistics, the NB-vs-Poisson
LR test, a log-likelihood comparison, (when fit) the CPB alpha and
ceiling-exceedance share, and the over-conditioning guard state:
\code{atrisk_skipped} (\code{TRUE} when the mean model nearly saturates the positive
counts, so the at-risk statistic is not computed and the printout says why),
\code{sat_ratio} (the fitted parameter share of the positives), and
\code{overconditioned} (\code{TRUE} when that share reaches 0.10, the region where the
calibrated threshold is anti-conservative; the printout then flags the
verdict as diagnostic rather than probative and recommends
\code{ztp_threshold = "bootstrap"}).
}
\description{
Applies the diagnostic sequence developed in Bagozzi (2026): a \emph{marginal} verdict
from the conditional Pearson statistic and a through-origin score regression (which
gives the direction of dispersion), a negative-binomial-versus-Poisson test for the
overdispersion call, and---most importantly---an \emph{at-risk} verdict on the positive
counts benchmarked against a \strong{zero-truncated Poisson}. The last step is what
separates genuine underdispersion from the artifact of conditioning on \eqn{Y>0}.
When the data are not zero-dominated it also fits the CPB and reports a
ceiling-exceedance diagnostic, and it fits a generalized-Poisson soft-tail
comparator.
}
\examples{
set.seed(1); x <- rnorm(250)
N <- pmax(round(exp(1.4 + 0.4 * x) / 0.6), 1); y <- rbinom(250, N, 0.6)
ud_screen(y ~ x, data = data.frame(y = y, x = x))
}
\references{
King, G. (1989). Variance specification in event count models.
\emph{AJPS} 33(3):762-784.

Bagozzi, B. E. (2026). Revisiting underdispersion in political science.
Companion manuscript.
}
\seealso{
\code{\link[=cpb]{cpb()}}, \code{\link[=compare_dispersion]{compare_dispersion()}}
}
