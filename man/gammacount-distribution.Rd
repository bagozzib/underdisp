% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_families.R
\name{gammacount-distribution}
\alias{gammacount-distribution}
\alias{dgammacount}
\alias{pgammacount}
\alias{qgammacount}
\alias{rgammacount}
\title{Gamma-count distribution functions}
\usage{
dgammacount(x, mu, alpha, log = FALSE)

pgammacount(q, mu, alpha, lower.tail = TRUE, log.p = FALSE)

qgammacount(p, mu, alpha, lower.tail = TRUE, log.p = FALSE)

rgammacount(n, mu, alpha)
}
\arguments{
\item{x, q}{Vector of quantiles (non-negative integers).}

\item{mu}{Rate parameter (scalar or vector, recycled).}

\item{alpha}{Dispersion parameter (scalar, positive).}

\item{log, log.p}{Return log probabilities.}

\item{lower.tail}{If \code{TRUE} (default), \eqn{P(X \le x)}.}

\item{p}{Vector of probabilities.}

\item{n}{Number of draws.}
}
\value{
\code{dgammacount} a density, \code{pgammacount} a CDF, \code{qgammacount} a
quantile, \code{rgammacount} a numeric vector of count draws.
}
\description{
Density, distribution, quantile, and random generation for Winkelmann's
(1995) gamma-count distribution: the number of events in a unit interval when
the waiting times between events are independent Gamma variables with shape
\code{alpha} and rate \code{alpha * mu}, so that \code{mu} is the long-run event rate.
\code{alpha > 1} (waiting times more regular than exponential) gives
underdispersion, \code{alpha = 1} is the Poisson, \code{alpha < 1} overdispersion; the
variance-to-mean ratio is approximately \code{1/alpha}. The exact mean and variance
are finite sums of incomplete-gamma terms and are what
\code{count_reg(family = "gammacount")} reports through \code{predict()} and
\code{fitted()}.
}
\examples{
dgammacount(0:5, mu = 3, alpha = 2)
var(rgammacount(2000, mu = 3, alpha = 2)) / 3
}
\references{
Winkelmann, R. (1995). Duration dependence and dispersion in
count-data models. \emph{Journal of Business & Economic Statistics}, 13(4),
467-474.
}
\seealso{
\code{\link[=count_reg]{count_reg()}}
}
