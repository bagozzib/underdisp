% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/count_families.R
\name{genpois-distribution}
\alias{genpois-distribution}
\alias{dgenpois}
\alias{pgenpois}
\alias{qgenpois}
\alias{rgenpois}
\title{Generalized Poisson distribution functions}
\usage{
dgenpois(x, mu, lambda, log = FALSE)

pgenpois(q, mu, lambda, lower.tail = TRUE, log.p = FALSE)

qgenpois(p, mu, lambda, lower.tail = TRUE, log.p = FALSE)

rgenpois(n, mu, lambda)
}
\arguments{
\item{x, q}{Vector of quantiles (non-negative integers).}

\item{mu}{Mean parameter (scalar or vector, recycled).}

\item{lambda}{Dispersion parameter in (-1, 1) (scalar).}

\item{log, log.p}{Return log probabilities.}

\item{lower.tail}{If \code{TRUE} (default), \eqn{P(X \le x)}.}

\item{p}{Vector of probabilities.}

\item{n}{Number of draws.}
}
\value{
\code{dgenpois} a density, \code{pgenpois} a CDF, \code{qgenpois} a quantile,
\code{rgenpois} a numeric vector of count draws.
}
\description{
Density, distribution, quantile, and random generation for the Consul-Jain
generalized Poisson distribution in its constant-dispersion form: mean \code{mu}
and dispersion \code{lambda} in (-1, 1), with
\eqn{P(Y = y) = \theta(\theta + \lambda y)^{y-1} e^{-\theta - \lambda y}/y!}
and \eqn{\theta = \mu(1 - \lambda)}, so that the variance-to-mean ratio is
\eqn{1/(1-\lambda)^2}. \code{lambda < 0} gives underdispersion, \code{lambda = 0} the
Poisson, \code{lambda > 0} overdispersion. For \code{lambda < 0} the support is finite
(the terms are positive only while \eqn{\theta + \lambda y > 0}) and the pmf is
renormalized on that support, so it is a proper distribution and the exact
mean and variance (which then differ from \code{mu} and \code{mu/(1-lambda)^2} only by
the negligible mass beyond the support) are what
\code{count_reg(family = "genpois")} reports through \code{predict()} and \code{fitted()}.
}
\examples{
dgenpois(0:5, mu = 3, lambda = -0.3)
var(rgenpois(2000, mu = 3, lambda = -0.3)) / 3   # about 1/(1.3)^2
}
\references{
Consul, P. C. and Jain, G. C. (1973). A generalization of the
Poisson distribution. \emph{Technometrics}, 15(4), 791-799. Consul, P. C.
and Famoye, F. (2006). \emph{Lagrangian Probability Distributions}.
Birkhauser.
}
\seealso{
\code{\link[=count_reg]{count_reg()}}
}
