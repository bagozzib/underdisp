% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gec_variants.R
\name{zi_gec}
\alias{zi_gec}
\title{Zero-inflated GEC (Katz-family) regression}
\usage{
zi_gec(
  formula,
  data,
  zero = NULL,
  zero_fe = NULL,
  se = c("none", "bootstrap"),
  B = 500,
  cluster = NULL,
  max.support = 500,
  maxit = 200,
  tol = 1e-06,
  offset = NULL
)
}
\arguments{
\item{formula}{Count formula, \code{y ~ x}.}

\item{data}{A data frame.}

\item{zero}{One-sided formula for the structural-zero logit; defaults to the
count formula's right-hand side.}

\item{zero_fe}{Optional column name for fixed effects in the zero-inflation
equation, entered as factor dummies (opt-in).}

\item{se}{Coefficient inference: \code{"none"} (default) or \code{"bootstrap"}.}

\item{B}{Bootstrap resamples.}

\item{cluster}{Optional cluster (column name or vector) for the bootstrap.}

\item{max.support}{Guard on the maximum evaluated support.}

\item{maxit, tol}{Optimizer controls.}

\item{offset}{Optional exposure offset (log scale) for the count component:
a numeric vector or the name of a column in \code{data}; the bootstrap carries
it through every replicate.}
}
\value{
An object of class \code{"zi_gec"}.
}
\description{
A structural-zero mixture with a GEC count component whose dispersion \code{delta}
is estimated freely. Estimated by robust direct maximum likelihood, seeded
from separate fits (a GEC on the positive counts for the count parameters and
a logit of the zero indicator for the inflation), exactly as in \code{\link[=zi_cpb]{zi_cpb()}};
this avoids the degenerate \code{pi -> 0} basin that coordinate-wise EM falls into.
}
\examples{
\donttest{
set.seed(4); n <- 300; x <- rnorm(n); z <- rnorm(n)
y <- ifelse(rbinom(n, 1, plogis(-0.5 + 0.8 * z)) == 1, 0L, rpois(n, exp(1 + 0.3 * x)))
zi_gec(y ~ x, data.frame(y = y, x = x, z = z), zero = ~ z)
}
}
\seealso{
\code{\link[=zi_cpb]{zi_cpb()}}, \code{\link[=gec]{gec()}}, \code{\link[=hurdle_gec]{hurdle_gec()}}
}
