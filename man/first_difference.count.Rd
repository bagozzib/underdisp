% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parity_methods.R, R/qoi_count.R
\name{first_difference.gec}
\alias{first_difference.gec}
\alias{first_difference.gec_fe}
\alias{first_difference.cpb_fe}
\alias{first_difference.hurdle_gec}
\alias{first_difference.zi_gec}
\alias{first_difference.count}
\alias{first_difference.count_reg}
\alias{first_difference.hurdle_count}
\alias{first_difference.zi_count}
\title{First differences for the matched count families}
\usage{
\method{first_difference}{gec}(object, variable, from, to, level = 0.95, ...)

\method{first_difference}{gec_fe}(object, variable, from, to, level = 0.95, ...)

\method{first_difference}{cpb_fe}(object, variable, from, to, level = 0.95, ...)

\method{first_difference}{hurdle_gec}(
  object,
  variable,
  from,
  to,
  level = 0.95,
  stage = c("both", "participation", "intensity", "zero", "count"),
  ...
)

\method{first_difference}{zi_gec}(
  object,
  variable,
  from,
  to,
  level = 0.95,
  stage = c("both", "participation", "intensity", "zero", "count"),
  ...
)

\method{first_difference}{count_reg}(object, variable, from, to, level = 0.95, ...)

\method{first_difference}{hurdle_count}(
  object,
  variable,
  from,
  to,
  level = 0.95,
  stage = c("both", "participation", "intensity", "zero", "count"),
  ...
)

\method{first_difference}{zi_count}(
  object,
  variable,
  from,
  to,
  level = 0.95,
  stage = c("both", "participation", "intensity", "zero", "count"),
  ...
)
}
\arguments{
\item{object}{A fitted \code{count_reg}, \code{gec}, \code{gec_fe}, \code{cpb_fe},
\code{hurdle_count}, \code{zi_count}, \code{hurdle_gec}, or \code{zi_gec} model.}

\item{variable}{Name of the covariate to change (must be in the model).}

\item{from, to}{The two values of \code{variable}.}

\item{level}{Confidence level for the delta-method intervals (all methods on
this page that can compute one).}

\item{...}{Unused; unknown arguments error.}

\item{stage}{For the two-part models, which equation(s) the change is applied
to. \code{"both"} (default) moves the variable wherever it appears;
\code{"intensity"}/\code{"count"} or \code{"participation"}/\code{"zero"} moves it in only
that equation, holding it at the reference in the other. For a variable in
only one equation all options coincide; for a variable in both, \code{"both"}
gives the total effect and the single-stage options the partial effect
through that margin. All component rows are always returned.}
}
\value{
A \code{"ud_fd"} data frame -- the package-wide first-difference contract
shared by every \code{first_difference()} method: columns \code{component}, \code{from},
\code{to}, \code{diff}, \code{lower}, \code{upper}, \code{method}. Single-equation fits return one
row (\code{component = "mean"}); two-part fits return one row per margin level
(binary-stage probability, count-stage mean, marginal mean). \code{method}
records the uncertainty source per row (\code{"delta"}, a bootstrap label, or
\code{"none"} with \code{NA} bounds).
}
\description{
The discrete-change effect on the expected count E(Y) as \code{variable} moves from
\code{from} to \code{to}, holding the other covariates at their sample means. For
\code{count_reg} this is a single number with a delta-method interval; for the
two-part models it is decomposed exactly into extensive (participation /
non-structural-zero) and intensive (count) channels that sum to the total.
}
