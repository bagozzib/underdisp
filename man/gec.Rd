% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gec.R
\name{gec}
\alias{gec}
\title{Generalized event count (Katz-family) regression}
\usage{
gec(
  formula,
  data,
  truncated = FALSE,
  se = c("none", "bootstrap"),
  B = 500,
  cluster = NULL,
  offset = NULL,
  max.support = 500,
  maxit = 20000,
  reltol = 1e-08
)
}
\arguments{
\item{formula}{A model formula.}

\item{data}{A data frame.}

\item{truncated}{Logical; if \code{TRUE}, fit the zero-truncated GEC (all \code{Y >= 1}),
the intensity model of \code{\link[=hurdle_gec]{hurdle_gec()}}.}

\item{se}{Coefficient inference: \code{"none"} (default; fast) or \code{"bootstrap"}.}

\item{B}{Bootstrap resamples when \code{se = "bootstrap"}.}

\item{cluster}{Optional cluster identifier (a column name in \code{data} or a
vector) for a cluster/block bootstrap; see \code{\link[=cpb]{cpb()}}.}

\item{offset}{Optional offset on the log-mean scale (an exposure): a numeric
vector or the name of a column in \code{data}.}

\item{max.support}{Guard on the maximum evaluated support.}

\item{maxit, reltol}{Optimizer controls.}
}
\value{
An object of class \code{"gec"} with \code{coefficients}, \code{delta} (the estimated
dispersion), \code{loglik}, bootstrap standard errors, and bookkeeping.
}
\description{
Fits King's generalized event count model, a Katz-family count regression
whose single dispersion parameter \code{delta} (the variance-to-mean ratio) is
estimated freely and spans underdispersion (\code{delta < 1}, a finite-support
member; the continuous parameter binomial is this cell), equidispersion
(\code{delta = 1}, Poisson), and overdispersion (\code{delta > 1}, the negative
binomial). Unlike \code{\link[=cpb]{cpb()}}, which fixes the direction of dispersion to
under, \code{gec()} lets the data choose. The Katz recursion delivers exact first
and second moments---the Winkelmann--Signorino--King correction realized
directly---and the likelihood is evaluated in C++.
}
\examples{
set.seed(1); x <- rnorm(400)
y <- rpois(400, exp(1 + 0.5 * x))
gec(y ~ x, data = data.frame(y = y, x = x), se = "none")
}
\seealso{
\code{\link[=cpb]{cpb()}}, \code{\link[=compare_dispersion]{compare_dispersion()}}
}
