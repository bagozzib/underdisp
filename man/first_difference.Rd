% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qoi.R
\name{first_difference}
\alias{first_difference}
\alias{first_difference.cpb}
\title{King-style first difference for a CPB fit}
\usage{
first_difference(object, ...)

\method{first_difference}{cpb}(
  object,
  variable,
  from,
  to,
  quantity = c("mean", "ceiling", "prob"),
  y = NULL,
  level = 0.95,
  ...
)
}
\arguments{
\item{object}{A \code{"cpb"} object fit with \code{se = "bootstrap"}.}

\item{...}{Further arguments passed to methods; unknown arguments error.}

\item{variable}{Name of a model-matrix column to vary.}

\item{from, to}{The two values of \code{variable} to contrast.}

\item{quantity}{\code{"mean"} (E(Y)), \code{"ceiling"} (lambda/(1-alpha)), or \code{"prob"}
(P(Y = \code{y})).}

\item{y}{The count value for \code{quantity = "prob"}.}

\item{level}{Confidence level (default 0.95).}
}
\value{
A \code{"ud_fd"} data frame -- the package-wide first-difference contract
(columns \code{component}, \code{from}, \code{to}, \code{diff}, \code{lower}, \code{upper}, \code{method}) --
with one row for the requested quantity and a bootstrap percentile
interval on the difference (\code{method = "bootstrap (stored)"}). Every
\code{first_difference()} method in the package returns this same shape.
}
\description{
The effect on a quantity of interest of moving one covariate \code{from} one value \code{to}
another, holding the other covariates at their means, with a bootstrap percentile
interval (Tomz, Wittenberg & King style, using the model's bootstrap draws).
}
\examples{
\donttest{
set.seed(1); x <- rnorm(400)
N <- pmax(round(exp(1.6 + 0.5 * x) / 0.5), 1); y <- rbinom(400, N, 0.5)
fit <- cpb(y ~ x, data = data.frame(y = y, x = x)[y > 0, ], se = "bootstrap", B = 200)
first_difference(fit, "x", from = -1, to = 1, quantity = "mean")
}
}
