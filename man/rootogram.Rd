% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{rootogram}
\alias{rootogram}
\title{Hanging rootogram for a fitted count model}
\usage{
rootogram(
  fit,
  kmax = NULL,
  main = "Rootogram",
  xlab = "Count",
  ylab = "sqrt(frequency)",
  ...
)
}
\arguments{
\item{fit}{A \code{"cpb"} or \code{"hurdle_cpb"} object.}

\item{kmax}{Highest count to display; defaults to the maximum observed count.}

\item{main, xlab, ylab}{Plot labels.}

\item{...}{Passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
Invisibly, a data frame of \code{count}, \code{observed}, and \code{expected}
frequencies.
}
\description{
Draws a Tukey hanging rootogram: bars for the observed frequencies hang from
the curve of expected frequencies, both on the square-root scale. Bars that
hang below the zero line mark counts the model under-predicts; bars that stop
short mark counts it over-predicts.
}
\examples{
set.seed(1)
y <- rcpb(400, lambda = 3, alpha = 0.5)
fit <- cpb(y ~ 1, data = data.frame(y = y), truncated = FALSE, se = "none")
rootogram(fit)
}
