% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{cv_score}
\alias{cv_score}
\title{Cross-validated proper scores for a count model}
\usage{
cv_score(fitfun, data, k = 5, kmax = NULL, folds = NULL)
}
\arguments{
\item{fitfun}{A function of one argument (a training data frame) returning a
fitted \code{underdisp} model, e.g.
\code{function(d) hurdle_cpb(y ~ x, d, participation = ~ z, fe = "unit", se = "none")}.}

\item{data}{The full data frame.}

\item{k}{Number of folds (default 5).}

\item{kmax}{Highest count to evaluate; if \code{NULL}, taken from a fit on the full
data (one extra fit).}

\item{folds}{Optional integer vector of length \code{nrow(data)} giving a fixed
fold assignment (so two models can be scored on identical folds). If \code{NULL},
folds are drawn at random.}
}
\value{
An object of class \code{"cv_score"}: a list with the mean held-out
\code{logscore} and \code{rps}, the per-observation vectors \code{logscore_i}/\code{rps_i}, and
the \code{folds} used.
}
\description{
K-fold cross-validated log score and RPS: the data are split into \code{k} folds,
the model is refit on each training set via \code{fitfun}, and its predicted
distribution is scored on the held-out fold. Because the score is genuinely
out of sample it penalizes over-parameterization, so it is the honest
criterion for comparing models that differ in the number of parameters (for
example a hurdle with per-unit participation fixed effects against a
zero-inflated model). Per-observation held-out scores are returned so two
models fit on the \emph{same} folds can be compared with a paired, cluster-robust
test.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(y = rcpb(500, lambda = 3, alpha = 0.5))
cv <- cv_score(function(tr) cpb(y ~ 1, tr, truncated = FALSE, se = "none"), d, k = 5)
cv$logscore
}
}
\seealso{
\code{\link[=score]{score()}}
}
