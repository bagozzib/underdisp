% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glm_families.R, R/parity_methods.R,
%   R/qoi_count.R
\name{irr.count_reg}
\alias{irr.count_reg}
\alias{irr.gec}
\alias{irr.gec_fe}
\alias{irr.cpb_fe}
\alias{irr.hurdle_gec}
\alias{irr.zi_gec}
\alias{irr.count}
\alias{irr.hurdle_count}
\alias{irr.zi_count}
\title{Rate and odds ratios for the matched count families}
\usage{
\method{irr}{count_reg}(object, level = 0.95, ...)

\method{irr}{gec}(object, level = 0.95, ...)

\method{irr}{gec_fe}(object, level = 0.95, ...)

\method{irr}{cpb_fe}(object, level = 0.95, ...)

\method{irr}{hurdle_gec}(object, level = 0.95, ...)

\method{irr}{zi_gec}(object, level = 0.95, ...)

\method{irr}{hurdle_count}(object, level = 0.95, ...)

\method{irr}{zi_count}(object, level = 0.95, ...)
}
\arguments{
\item{object}{A fitted model from this package.}

\item{level}{Confidence level.}

\item{...}{Unused.}
}
\value{
A \code{"ud_irr"} data frame -- the package-wide ratio contract shared by
every \code{irr()} method: columns \code{term}, \code{equation} (\code{"count"},
\code{"participation"} for a hurdle's binary stage, or \code{"inflation"} for a
zero-inflated model's structural-zero stage; the two point in opposite
directions, an odds ratio above one raising P(Y > 0) in the first and
P(structural zero) in the second), \code{ratio} (\code{"IRR"} or \code{"OR"}),
\code{estimate}, \code{lower}, \code{upper}, and \code{method} (the interval source; \code{"none"}
with \code{NA} bounds when no covariance is available). Two-part models stack
both equations. The \code{IRR} rows are ratios of the rate parameter
\eqn{\exp(\beta)}; for the CPB and the underdispersed GEC the ratio of
fitted means differs from it by the support renormalization, and
\code{\link[=first_difference]{first_difference()}} reports the exact mean contrast.
}
\description{
Rate ratios (\code{exp} of the count/intensity coefficients, column \code{IRR}) and, for
the two-part models, odds ratios of the binary stage (column \code{OR}), by
equation. Element names match the CPB family (\code{intensity} for the count
component; \code{participation} for the hurdle stage, \code{zero} for the inflation
stage).
}
