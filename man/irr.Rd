% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qoi.R
\name{irr}
\alias{irr}
\alias{irr.cpb}
\title{Incidence rate ratios for a CPB fit}
\usage{
irr(object, ...)

\method{irr}{cpb}(object, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{"cpb"} object fit with \code{se = "bootstrap"}.}

\item{...}{Further arguments passed to methods.}

\item{level}{Confidence level (default 0.95).}
}
\value{
A \code{"ud_irr"} data frame -- the package-wide rate-ratio contract
(columns \code{term}, \code{equation}, \code{ratio}, \code{estimate}, \code{lower}, \code{upper},
\code{method}) shared by every \code{irr()} method; here with bootstrap percentile
intervals from the stored draws.
}
\description{
Incidence rate ratios for a CPB fit
}
\examples{
\donttest{
set.seed(8); x <- rnorm(300)
N <- pmax(round(exp(1.5 + 0.4 * x) / 0.5), 1); y <- rbinom(300, N, 0.5)
fit <- cpb(y ~ x, data.frame(y = y, x = x)[y > 0, ], se = "bootstrap", B = 100)
irr(fit)
}
}
